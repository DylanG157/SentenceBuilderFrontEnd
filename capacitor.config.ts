import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'word-app',
  webDir: 'dist/word-app',
  bundledWebRuntime: false,
};

export default config;
